from setuptools import setup, find_packages
from codecs import open
from os import path

here = path.abspath(path.dirname( __file__ ))

setup(
    name='appservice-flask',
    version='1.0.0',
    description='Python basic flask app',
    license='Apache-2.0'
)